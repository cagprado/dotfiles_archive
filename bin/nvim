#!/bin/zsh

if [[ -x /usr/bin/nvim ]]; then
  # temporary fix for del/insert in neovim
  [[ ! $TERM =~ "linux" ]] && echoti smkx || :
  /usr/bin/nvim $@
  [[ ! $TERM =~ "linux" ]] && echoti rmkx || :
else
  vim $@
fi
