#!/bin/zsh

local DESC="Test if physical location is sampa cluster (ssh)."
local USAGE="%Busage:%b $0"

if [[ "$1" = "-h" ]]; then
  print -P "$USAGE\n$DESC"
  return
elif [[ "$1" = "-H" ]]; then
  print -P -f "%${LIST_WIDTH}s  %s\n" "%B$0%b" "$DESC"
  return
fi

[[ "$AT_SAMPA_VALUE" = "true" ]]
